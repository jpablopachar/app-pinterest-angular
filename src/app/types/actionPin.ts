/**
 * Representa las acciones posibles que se pueden realizar sobre un pin.
 * 
 * - `'like'`: Indica que el usuario ha dado "me gusta" al pin.
 * - `'save'`: Indica que el usuario ha guardado el pin.
 */
export type ActionPin = 'like' | 'save';